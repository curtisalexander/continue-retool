{
  "schema_version": 1,
  "servers": [
    {"name":"shell","module":"shell_mcp","default":true,"summary":"Terminal runner with background jobs, tree-kill, and timeouts","site_description":"Terminal runner with background jobs, tree-kill, bounded output, and timeouts.","responsibility":"Foreground and background commands, polling, input, cancellation","authority":"Arbitrary subprocesses; keep human-approved","policy":"shell.* -> Ask First"},
    {"name":"fs","module":"fs_mcp","default":true,"summary":"Bounded line reads and directory listings","site_description":"Bounded line reads and directory listings; replaces built-in Read and List dir.","responsibility":"Bounded line reads and directory listings","authority":"Read access with defense-in-depth workspace path scoping","policy":"fs.* -> Automatic if your threat model permits"},
    {"name":"search","module":"search_mcp","default":true,"summary":"ripgrep-backed content and file search","site_description":"ripgrep-backed content and file search; replaces built-in Grep and Glob.","responsibility":"Content and file search through ripgrep","authority":"Read access with defense-in-depth workspace path scoping; spawns rg","policy":"search.* -> Automatic if your threat model permits"},
    {"name":"edit","module":"edit_mcp","default":true,"summary":"Atomic Unicode-tolerant file editing","site_description":"Atomic, Unicode-tolerant edit and create operations.","responsibility":"Atomic create/edit with Unicode-tolerant matching","authority":"File mutation with defense-in-depth workspace path scoping","policy":"edit.* -> Ask First"},
    {"name":"sql","module":"sql_mcp","default":false,"summary":"Optional SQL formatting and linting through sqruff","site_description":"Optional SQL formatting and linting through sqruff, with Snowflake-oriented defaults.","responsibility":"SQL formatting and linting through sqruff","authority":"SQL strings and a subprocess; no file-path tool input","policy":"sql.* -> Automatic"}
  ]
}
