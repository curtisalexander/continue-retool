# Drop into your workspace's .continue/mcpServers/ directory.
#
# Then in Continue's Agent tool settings:
#   * set the built-in  Edit file    policy to  Excluded
#   * set the built-in  Create file  policy to  Excluded
#   * set  edit.edit / edit.multi_edit / edit.create_file  to  Ask First
#     (these write to disk — keep a human in the loop until you trust them)
name: edit
version: 0.0.1
schema: v1
mcpServers:
  - name: edit
    command: uv
    # --project pins the server's uv environment to its package dir, so `cwd` is
    # NOT needed for the server itself. Set MCP_WORKSPACE to your workspace root:
    # it's the base for relative paths.
    args: ["run", "--project", "/absolute/path/to/continue-mcp/edit-mcp", "edit-mcp"]
    env:
      MCP_WORKSPACE: /absolute/path/to/your/workspace
