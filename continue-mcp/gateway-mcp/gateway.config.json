{
  "_comment": "Downstream MCP servers the gateway aggregates. Continue connects ONLY to the gateway; the gateway connects to these. Server names must not contain '_'. Relative cwd entries resolve against this file's directory.",
  "servers": {
    "shell": {
      "command": "uv",
      "args": ["run", "shell-mcp"],
      "cwd": "../shell-mcp"
    },
    "search": {
      "command": "uv",
      "args": ["run", "search-mcp"],
      "cwd": "../search-mcp"
    },
    "edit": {
      "command": "uv",
      "args": ["run", "edit-mcp"],
      "cwd": "../edit-mcp"
    }
  }
}
