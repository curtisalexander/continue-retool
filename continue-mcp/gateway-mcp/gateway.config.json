{
  "_comment": "Generated from servers.json. Continue connects only to the gateway; relative cwd entries resolve against this file's directory.",
  "servers": {
    "sql": {
      "command": "uv",
      "args": [
        "run",
        "--no-sync",
        "--project",
        ".",
        "sql-mcp"
      ],
      "cwd": ".."
    },
    "notes": {
      "command": "uv",
      "args": [
        "run",
        "--no-sync",
        "--project",
        ".",
        "notes-mcp"
      ],
      "cwd": ".."
    }
  }
}
